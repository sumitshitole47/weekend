# ==============================================================================
# AeroTwin-3D Project Dependencies
#
# IMPORTANT (CUDA GPU Support):
# To ensure PyTorch utilizes your NVIDIA GPU with CUDA acceleration instead of
# silently installing the CPU-only build, install PyTorch with the CUDA index:
#
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
# ==============================================================================

--extra-index-url https://download.pytorch.org/whl/cu126

# Deep Learning & Object/Instance Segmentation
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0

# 3D Computer Vision, Point Clouds & Geometry
opencv-python>=4.8.0
open3d>=0.17.0
numpy>=1.24.0
scipy>=1.10.0

# Web Application & API Server
fastapi>=0.100.0
uvicorn>=0.22.0
python-multipart>=0.0.6
pyyaml>=6.0
tqdm>=4.65.0
